{
  "name": "custom-mcp-server",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "format": "turbo format",
    "lint": "turbo lint",
    "check-types": "turbo check-types",
    "run:server": "bun run apps/server/dist/index.js",
    "run:client": "bun run apps/client/dist/index.js"
  },
  "type": "module",
  "devDependencies": {
    "oxlint": "^1.2.0",
    "prettier": "^3.5.3",
    "turbo": "^2.5.4"
  },
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "bun@1.2.4",
  "dependencies": {
    "zod": "^3.25.67"
  }
}
