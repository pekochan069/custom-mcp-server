{
  "name": "server",
  "type": "module",
  "module": "./src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun run build.ts",
    "format": "prettier --write .",
    "lint": "oxlint --fix .",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.2.16",
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3"
  }
}
